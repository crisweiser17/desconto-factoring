# Tasks
- [x] Task 1: Remover dropdown de seleção de titularidade bancária (`#titular_selecao`) e seu label associado em `form_cedente.php`.
- [x] Task 2: Modificar a inicialização de campos para que `#conta_titular` e `#conta_documento` recebam o atributo `readonly`.
- [x] Task 3: Modificar o script JavaScript em `form_cedente.php` para sincronizar os valores de `#empresa` com `#conta_titular` e `#documento_principal` com `#conta_documento` sempre que os primeiros mudarem (em tempo real).
- [x] Task 4: Modificar o script para preencher os dados bancários na carga inicial da página (se não houver valor, ou apenas forçar sincronização).
- [x] Task 5: Validar que a seleção de Representante (`#representante_socio_select`) continua funcionando e permite selecionar qualquer um dos sócios cadastrados.
