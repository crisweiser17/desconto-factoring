# Tasks

- [x] Task 1: Remover o vínculo global do `style.css` do arquivo `fechamento.php` para impedir sobrescritas na classe `.container`.
- [x] Task 2: Verificar a página e certificar-se de que as propriedades dos cartões e dos componentes de fechamento se mantêm adequadas usando utilitários do Bootstrap, sem quebrar o layout.